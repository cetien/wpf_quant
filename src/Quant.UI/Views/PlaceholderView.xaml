<UserControl x:Class="Quant.UI.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1E1E2E">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.25">
            <TextBlock x:Name="TxtIcon" Text="◌" FontSize="64"
                       Foreground="#89B4FA" HorizontalAlignment="Center"
                       FontFamily="Consolas"/>
            <TextBlock x:Name="TxtLabel" Text=""
                       FontSize="18" FontWeight="SemiBold"
                       Foreground="#CDD6F4" HorizontalAlignment="Center"
                       Margin="0,12,0,6" FontFamily="Consolas"/>
            <TextBlock Text="준비 중입니다"
                       FontSize="12" Foreground="#6C7086"
                       HorizontalAlignment="Center" FontFamily="Consolas"/>
        </StackPanel>
    </Grid>
</UserControl>
